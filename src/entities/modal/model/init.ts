import { ModalInitState } from "./types";


const initialState: ModalInitState = {
  isActive: true,
  title: null,
  variant: null,
  subTitle: null,
  buttonText: null,
  approval: false,
}

export { initialState }