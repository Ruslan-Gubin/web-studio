import { ModalInitState } from "./types";


const initialState: ModalInitState = {
  isActive: false,
  title: null,
  variant: null,
  subTitle: null,
  buttonText: null,
  approval: false,
}

export { initialState }